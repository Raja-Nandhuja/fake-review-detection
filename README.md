# Smart Fake Review Detection System

## Steps to Run

1. Install Python 3.9+
2. Install dependencies:
   pip install -r requirements.txt

3. Train model:
   python train.py

4. Run server:
   python app.py

5. Test API (POST):
   http://127.0.0.1:5000/predict
